import * as React from "react";

import { Layout } from "app/components/Layout";
import { DeparturePage } from "app/pages";

const Departure: React.FC = () => {
  return (
    <Layout>
      <DeparturePage />
    </Layout>
  );
};

export default Departure;
