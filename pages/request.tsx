import * as React from "react";

import { Layout } from "app/components/Layout";
import { RequestPage } from "app/pages";

const Request = () => {
  return (
    <Layout>
      <RequestPage />
    </Layout>
  );
};

export default Request;
