import * as React from "react";

import { Layout } from "app/components/Layout";
import { MainPage } from "app/pages";

const Main = () => {
  return (
    <Layout>
      <MainPage />
    </Layout>
  );
};

export default Main;
